import { Aspect } from '@oliveoilexpert/stim'

export default class Root extends Aspect {
    static attributes = {}
    static aspects = [
        'anchor-scrolling',
        'scrollbar-width'
    ]
}